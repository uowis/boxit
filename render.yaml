services:
  - type: web
    name: patron-plie
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: donnees
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /var/data
